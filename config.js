// Checkout settings. This is the only file you need to edit in your fork.
window.BKASH_CONFIG = {
  // URL of your deployed payment Worker (see worker/ and setup.html), e.g.
  // 'https://bkash-blogger.yourname.workers.dev'.
  // Leave it empty to run the checkout in demo mode: the full flow works,
  // but no real payment is made.
  apiBase: '',

  // Your shop or blog name, shown to customers in the checkout popup.
  merchantName: 'Demo Store',
};
